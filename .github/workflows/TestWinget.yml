name: Test Winget

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Check winget
        shell: pwsh
        run: |
          winget search --id Microsoft.WingetCreate -s winget
